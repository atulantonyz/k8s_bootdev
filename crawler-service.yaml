kind: "Service"
apiVersion: "v1"
metadata:
  name: "crawler-service"
  namespace: crawler
spec:
  selector:
    app: "synergychat-crawler"
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080

